<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- État pressé : ajoute un filtre blanc semi-opaque -->
    <item android:state_pressed="true">
        <layer-list>
            <!-- Couche de base : l'arrière-plan par défaut -->
            <item>
                <shape android:shape="rectangle">
                    <corners android:radius="16dp" />
                    <solid android:color="@android:color/transparent" />
                </shape>
            </item>
            <!-- Filtre blanc semi-opaque -->
            <item>
                <shape android:shape="rectangle">
                    <corners android:radius="16dp" />
                    <solid android:color="#80FFFFFF" /> <!-- Blanc avec opacité à 50% -->
                </shape>
            </item>
        </layer-list>
    </item>

    <!-- État par défaut -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="16dp" />
        </shape>
    </item>
</selector>
